[package]
name = "html-traits"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
html = { path = "../html" }

[dev-dependencies]
html-derive = { path = "../html-derive" }
